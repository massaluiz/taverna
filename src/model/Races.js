export default class Races {


    constructor(name, slug, desc, asi_desc,
                age, alignment,
                size) {

        this.name = name;
        this.slug = slug;
        this.desc = desc;
        this.asi_desc = asi_desc;
        this.age = age;
        this.alignment = alignment;
        this.size = size;
        

    }
}